python3 <<'PYEOF'
from pathlib import Path

content = '''# CLAIMS

We claim:

1. A computer-implemented system for stabilizing automated decisions derived from optical character recognition (OCR) output, the system comprising:

   a memory; and

   one or more processors configured to:

   (a) extract a set of semantic fields from OCR output of a document, each extracted field having associated provenance data comprising a source token and an OCR confidence value;

   (b) construct a Decision Dependency Graph comprising a mapping from each semantic field of the set to one or more named conditions of a downstream decision function, and a set of condition evaluators derived from the downstream decision function;

   (c) detect, for each extracted field, a semantic uncertainty based at least on the OCR confidence value of the source token or on a format ambiguity of the source token;

   (d) generate, for each field determined to be uncertain and decision-relevant according to the Decision Dependency Graph, a set of constrained counterfactual interpretations, wherein the set is generated by applying at least one of an OCR confusion pattern and a format rule to the source token, and wherein the set is augmented with one or more probe values spanning a decision boundary of the downstream decision function when the OCR confidence value is below a threshold or when the field is absent from the set of semantic fields;

   (e) compute a Counterfactual Decision Impact Score for each uncertain field by evaluating the downstream decision function on the constrained counterfactual interpretations and computing a normalized weighted entropy over resulting decision labels;

   (f) determine a minimum-cost verification set V* by solving a constrained optimization problem that minimizes a total verification cost over subsets V of the uncertain fields, subject to a decision-invariance constraint requiring that every remaining interpretation of the unverified uncertain fields yields a same downstream decision after fields in V are replaced with verified values; and

   (g) execute one or more verification actions on fields in V* and terminate verification when the downstream decision becomes invariant.

2. The system of claim 1, wherein the Decision Dependency Graph is constructed from a declarative mapping of field names to condition names, and wherein the condition evaluators are imported from the downstream decision function.

3. The system of claim 1, wherein the Counterfactual Decision Impact Score is zero for a field that is not decision-relevant according to the Decision Dependency Graph, irrespective of the semantic uncertainty of the field.

4. The system of claim 1, wherein the constrained optimization problem is solved by exhaustive enumeration of subsets of the uncertain fields in order of increasing cardinality.

5. The system of claim 1, wherein the one or more verification actions are executed sequentially, and wherein after each verification action the system recomputes remaining candidate interpretations and selects a next field to verify by ranking remaining uncertain fields by descending Counterfactual Decision Impact Score and, as a tie-break, ascending verification cost.

6. The system of claim 1, wherein the one or more probe values comprise a first value below a decision threshold of the downstream decision function and a second value above the decision threshold.

7. The system of claim 1, wherein the total verification cost is determined from a cost table assigning a cost to each semantic field.

8. A computer-implemented method for stabilizing automated decisions derived from OCR output, the method comprising:

   extracting a set of semantic fields from OCR output of a document, each extracted field having associated provenance data comprising a source token and an OCR confidence value;

   constructing a Decision Dependency Graph mapping each semantic field to one or more conditions of a downstream decision function;

   detecting a semantic uncertainty for one or more extracted fields based at least on the OCR confidence value or on a format ambiguity;

   generating constrained counterfactual interpretations for each uncertain field that is decision-relevant according to the Decision Dependency Graph, wherein the interpretations are generated by applying at least one of an OCR confusion pattern and a format rule, and wherein the interpretations are augmented with probe values spanning a decision boundary when the OCR confidence value is below a threshold or when the field is absent;

   computing a Counterfactual Decision Impact Score for each uncertain field by evaluating the downstream decision function on the counterfactual interpretations;

   determining a minimum-cost verification set V* by minimizing a total verification cost over subsets of the uncertain fields subject to a decision-invariance constraint; and

   executing verification actions on fields in V* and terminating verification when the downstream decision becomes invariant.

9. A non-transitory computer-readable medium storing instructions that, when executed by one or more processors, cause the one or more processors to perform the method of claim 8.
'''

Path("docs/claims.md").write_text(content, encoding="utf-8")
print("Wrote docs/claims.md:", len(content.splitlines()), "lines")
PYEOF